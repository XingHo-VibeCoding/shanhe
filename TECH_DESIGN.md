# 诗迹山河 · 技术设计文档（TECH_DESIGN.md）

> 项目：诗迹山河（古诗词学习平台，核心特色「地图 + 诗词」）
> 文档版本：v1.0
> 日期：2026-09-18
> 本文档是 Day 5 的技术选型与技术设计文档，为 Day 6/7 的代码开发提供依据。

---

## 一、技术路线（一句话）

**前端 Vue（看界面） + 后端 Python/Flask（算逻辑） + 数据库 SQLite（存数据）**

### 选型理由（为什么这么选）

| 层 | 选型 | 为什么选它 | 备选方案 |
|---|---|---|---|
| 前端 | Vue | 交互多（图层筛选、地图联动），Vue 管理页面状态更省力 | React；纯 HTML/CSS/JS |
| 后端 | Python + Flask | 想用 Python 写后端；Flask 是 Python 里最轻量的 Web 框架，易学易部署 | Node.js + Express |
| 数据库 | SQLite | 文件型数据库，零配置、免装服务器，28 天 MVP 数据量小（精选几十首诗）完全够用 | MySQL、PostgreSQL |

> 说明：本方案是「前端 + 后端 + 数据库」三层分离架构。相比「纯前端（数据写死在 JSON 文件）」方案，本方案更专业、可扩展，后续能平滑加入登录、评论、用户上传等功能。

---

## 二、前后端与数据库分工

- **前端（Vue）**：负责用户看得见、点得到的界面——页面布局、诗词卡片、地图标记、筛选面板、播放按钮等。只做「展示」和「发送请求」，不存数据、不算逻辑。
- **后端（Python + Flask）**：负责「加工需求」——接收前端请求，去数据库取数据，处理筛选/排序等逻辑，把结果返回给前端。
- **数据库（SQLite）**：负责「存数据」——诗词原文、释义、赏析、诗人简介、地点坐标等，全部存在这里。

一句话：**前端管「看」，后端管「算」，数据库管「存」。**

---

## 三、数据流图

```
┌─────────────┐    请求(查诗词/地点)    ┌─────────────┐    查询语句    ┌─────────────┐
│             │ ─────────────────────▶ │             │ ────────────▶ │             │
│  前端 Vue   │                        │  后端 Flask  │               │  数据库     │
│  (用户界面)  │ ◀───────────────────── │  (加工逻辑)  │ ◀──────────── │  SQLite     │
│             │     返回数据(JSON)      │             │    数据结果    │  (存数据)    │
└─────────────┘                        └─────────────┘               └─────────────┘
```

### 数据流向文字说明（从哪来、到哪去）

1. **用户在浏览器（前端 Vue）操作**：如点击地图上的「黄鹤楼」、或输入「李白」搜索。
2. **前端向后端发起请求**：如「请给我地点=黄鹤楼的所有诗词」。
3. **后端接收请求并查询数据库**：后端用 SQL 从 SQLite 里查出符合条件的诗词/诗人/地点数据。
4. **数据库返回原始数据给后端**。
5. **后端加工数据**：做排序、筛选、格式化，打包成 JSON。
6. **后端把结果返回给前端**。
7. **前端把数据显示给用户**：渲染成诗词列表、地图标记、详情卡片。

> **一句话说清数据从哪来、到哪去**：数据存在 SQLite 数据库里，由 Python 后端读取加工，再通过接口传给 Vue 前端，最终显示在用户浏览器页面上。

---

## 四、数据如何存储（数据库结构规划）

SQLite 里规划三张表（后续开发时落地）：

| 表名 | 存什么 | 关键字段举例 |
|---|---|---|
| `poems`（诗词） | 诗词原文、释义、赏析、朝代、主题 | id、标题、作者、朝代、正文、首联、释义、简析、主题 |
| `poets`（诗人） | 诗人生平简介、代表作 | id、姓名、字号、生卒年、籍贯、官职、流派、简介 |
| `places`（地点） | 地点信息、坐标、相关诗词 | id、地名、省份、经纬度、历史简介、争议标注 |

三张表通过「作者」「地点」等字段关联，支撑诗词查询、地图标记、行迹轨迹等功能。

---

## 五、各功能模块的技术落地（对照 PRD）

| PRD 功能 | 技术落地方式 |
|---|---|
| 诗词查询 | 后端提供 `/api/poems?dynasty=唐` 或 `?poet=李白` 接口，前端调用展示 |
| 静态诗词地图 | 前端加载中国地图静态图，用 SVG 叠加地点标记点（坐标存 places 表） |
| 诗人行迹轨迹 | 地点坐标 + 时间顺序，前端用 SVG 折线连接，带箭头 |
| 图层/分类切换 | 前端 Vue 状态管理筛选条件，后端按朝代/主题过滤返回 |
| 地点详情页 | 后端 `/api/places/:id` 返回地点 + 关联诗词 |
| 争议地点标注 | places 表加「争议」字段，前端用 `*` 号 + 弹框展示 |
| 语音播放 | 真人音频文件（MP3）+ 浏览器 Web Speech API 兜底，前端实现高亮 |
| 诗人简介 | poets 表数据，前端渲染为结构化卡片 |
| 官方配图 | 静态图片文件，前端在详情页展示 |

---

## 六、目录结构规划（Day 6/7 落地）

```
vibe coding/
├── frontend/            # 前端（Vue）
│   ├── src/             # 页面组件、逻辑
│   └── ...
├── backend/             # 后端（Python + Flask）
│   ├── app.py           # Flask 入口
│   ├── database.db      # SQLite 数据库文件
│   └── ...
├── data/                # 数据（诗词/诗人/地点原始数据，导入数据库用）
├── assets/              # 静态资源（地图图、诗词配图、音频）
├── index.html           # 入口页
├── research.md          # Day 3 需求研究
├── PRD.md               # Day 4 产品需求
├── TECH_DESIGN.md       # Day 5 本文档
└── .gitignore           # 忽略敏感文件
```

---

## 七、技术栈说明（给初学者）

- **前端 Vue**：一个 JavaScript 框架，帮你管理页面数据和交互，界面变复杂时比手写 JS 更省力。
- **后端 Python + Flask**：Python 是一门好上手的语言；Flask 是它最轻量的 Web 框架，几十行就能跑起接口。
- **数据库 SQLite**：一个「文件就是数据库」的轻量数据库，无需安装服务器，适合小项目。
- **数据交互格式 JSON**：前后端之间传数据的通用格式，人类可读、结构清晰。

---

## 八、本期不做（技术层面）

- 不引入 React（已选 Vue）
- 不使用 MySQL/PostgreSQL 等需单独部署的数据库（用 SQLite）
- 不引入复杂前端构建链（如 Webpack/Vite 的复杂配置），保持轻量
- 不实现用户认证、不接第三方登录
